<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fast.modules.system.mapper.DeptMapper">

    <!-- 根据角色ID查询部门ID列表 -->
    <select id="selectDeptIdsByRoleId" resultType="Long">
        SELECT d.dept_id
        FROM sys_role_dept d
        WHERE d.role_id = #{roleId}
    </select>

    <!-- 根据部门ID查询子部门ID列表(包含本部门) -->
    <select id="selectChildrenDeptIdsById" resultType="Long">
        SELECT d.id
        FROM sys_dept d
        WHERE d.del_flag = '0'
          AND (d.id = #{deptId} OR d.ancestors LIKE '%' || #{deptId} || '%')
    </select>

</mapper>