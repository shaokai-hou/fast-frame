<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fast.modules.system.mapper.DictTypeMapper">

    <!-- 分页查询字典类型列表 -->
    <select id="selectDictTypePage" resultType="com.fast.modules.system.domain.vo.DictVO">
        SELECT id, dict_name, dict_type, status, remark, create_time
        FROM sys_dict_type
        WHERE del_flag = '0'
        <if test="query.dictName != null and query.dictName != ''">
            AND dict_name LIKE CONCAT('%', #{query.dictName}, '%')
        </if>
        <if test="query.dictType != null and query.dictType != ''">
            AND dict_type LIKE CONCAT('%', #{query.dictType}, '%')
        </if>
        <if test="query.status != null and query.status != ''">
            AND status = #{query.status}
        </if>
        ORDER BY create_time DESC
    </select>

</mapper>