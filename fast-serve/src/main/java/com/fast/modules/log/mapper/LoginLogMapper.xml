<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fast.modules.log.mapper.LoginLogMapper">

    <!-- 分页查询登录日志列表 -->
    <select id="selectLoginLogPage" resultType="com.fast.modules.log.domain.vo.LoginLogVO">
        SELECT id, username, ip_address, login_location, browser, os, status, msg, login_time
        FROM sys_login_log
        <where>
            <if test="query.username != null and query.username != ''">
                AND username LIKE CONCAT('%', #{query.username}, '%')
            </if>
            <if test="query.status != null and query.status != ''">
                AND status = #{query.status}
            </if>
        </where>
        ORDER BY login_time DESC
    </select>

</mapper>